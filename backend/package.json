{
  "name": "no-gas-labs-ai-guild-platform-backend",
  "version": "2.0.0",
  "private": true,
  "description": "Fail-closed read-only production baseline for the No Gas Labs AI Guild platform.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check runtime-config.js && node --check config/database.js && node --check routes/auth.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "body-parser": "^2.3.0",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.1.0",
    "ioredis": "^5.8.2",
    "jsonwebtoken": "^9.0.3",
    "path-to-regexp": "^8.4.2",
    "pg": "^8.16.3",
    "qs": "^6.15.3"
  }
}
